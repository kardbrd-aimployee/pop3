---
phase: 02-economy-and-combat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/terrain/mod.rs
  - src/engine/terrain/modify.rs
  - src/engine/terrain/cascade.rs
  - src/engine/terrain/water.rs
  - src/engine/mod.rs
autonomous: true
requirements: [TERR-01, TERR-02, TERR-03]

must_haves:
  truths:
    - "Terrain height can be modified gradually per tick"
    - "After height modification, cascade updates normals, walkability, water, pathfinding, and mesh"
    - "Cells transition between water and land based on height vs water level"
  artifacts:
    - path: "src/engine/terrain/modify.rs"
      provides: "Terrain_ModifyHeight function"
      contains: "pub fn modify_height"
    - path: "src/engine/terrain/cascade.rs"
      provides: "Full cascade after terrain modification"
      contains: "pub fn terrain_cascade"
    - path: "src/engine/terrain/water.rs"
      provides: "Water/land transition logic"
      contains: "pub fn update_water_cells"
  key_links:
    - from: "src/engine/terrain/modify.rs"
      to: "src/engine/terrain/cascade.rs"
      via: "modify_height calls terrain_cascade"
      pattern: "terrain_cascade"
    - from: "src/engine/terrain/cascade.rs"
      to: "src/engine/terrain/water.rs"
      via: "cascade calls update_water_cells"
      pattern: "update_water_cells"
---

<objective>
Implement terrain modification (raise/lower height) with the full cascade that updates all derived data. This is reused by spells in Phase 3 and needed for building placement terrain flattening validation.

Purpose: Terrain modification is foundational -- buildings interact with terrain, spells modify it, water responds to it.
Output: src/engine/terrain/ module with modify_height, terrain_cascade, and water interaction functions.
</objective>

<execution_context>
@.planning/phases/02-economy-and-combat/02-02-PLAN.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-economy-and-combat/2-CONTEXT.md
@.planning/phases/02-economy-and-combat/02-RESEARCH.md

<interfaces>
From src/render/terrain.rs (LandscapeMesh):
```rust
pub struct LandscapeMesh<const N: usize> {
    pub heights: Box<[[u16; N]; N]>,
    pub normals: Box<[[[f32; 3]; N]; N]>,
    // ... mesh data, shift offsets, etc.
}
// Existing methods: recalculate_normals(), flatten_building_footprint(), smooth_terrain_area()
```

From src/engine/movement/region.rs:
```rust
pub struct RegionMap { cells: Box<[[RegionCell; 128]; 128]> }
// RegionCell has walkability flags including CELL_HAS_BUILDING, water flags
```

From docs/specs/terrain.md:
- Terrain_ModifyHeight at 0x4ea2e0: gradual height change, delta applied per tick
- Cascade order: heights -> normals -> walkability -> buildings -> water -> pathfinding -> mesh
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Terrain height modification and cascade pipeline</name>
  <files>
    src/engine/terrain/mod.rs,
    src/engine/terrain/modify.rs,
    src/engine/terrain/cascade.rs,
    src/engine/terrain/water.rs
  </files>
  <read_first>
    src/render/terrain.rs (first 150 lines for LandscapeMesh struct and height access patterns),
    src/engine/movement/region.rs (for RegionMap walkability update patterns),
    docs/specs/terrain.md (Terrain_ModifyHeight at 0x4ea2e0, cascade order)
  </read_first>
  <behavior>
    - modify_height(heights, x, y, target_height, rate) adjusts height[y][x] toward target_height by rate per call
    - modify_height returns true if height actually changed
    - modify_height_area applies modify_height to a radius around center
    - terrain_cascade takes affected cell range and updates: normals, walkability, water
    - update_water_cells marks cells as water if height < water_level, land otherwise
    - Toroidal wrapping: all x,y coordinates wrap around 128x128 grid
  </behavior>
  <action>
    Create src/engine/terrain/ module with 4 files:

    **modify.rs**:
    - `pub fn modify_height(heights: &mut [[u16; 128]; 128], x: usize, y: usize, target: u16, rate: u16) -> bool` -- moves heights[y][x] toward target by at most rate per call. Returns true if height changed. Wraps x,y with `& 127`.
    - `pub fn modify_height_area(heights: &mut [[u16; 128]; 128], cx: usize, cy: usize, radius: usize, target: u16, rate: u16) -> bool` -- applies modify_height to all cells within radius of (cx,cy), toroidal distance. Returns true if any height changed.
    - `pub fn flatten_area(heights: &mut [[u16; 128]; 128], cx: usize, cy: usize, radius: usize) -> bool` -- computes average height in area, then modify_height_area toward that average. Returns true if any changed.
    - After any modification that returns true, caller must invoke terrain_cascade.

    **cascade.rs**:
    - `pub struct CascadeRegion { pub min_x: usize, pub min_y: usize, pub max_x: usize, pub max_y: usize }` -- affected cell range (wrapping handled internally)
    - `pub fn terrain_cascade(heights: &[[u16; 128]; 128], region: &CascadeRegion, normals: &mut [[[f32; 3]; 128]; 128], walkability: &mut [[u8; 128]; 128], water_level: u16) -> CascadeResult`
    - CascadeResult contains: `normals_updated: bool, walkability_changed: Vec<(usize, usize)>, water_cells_changed: Vec<(usize, usize)>, mesh_dirty: bool`
    - Cascade steps in order:
      1. Recalculate normals for cells in region (cross-product of adjacent height differences)
      2. Update walkability flags (steep slopes become unwalkable, water cells unwalkable)
      3. Update water cells (call update_water_cells)
      4. Mark mesh as dirty (return flag for renderer)
    - Note: pathfinding segment invalidation is a stub for now (SegmentPool invalidation will be wired when pathfinding is affected by terrain mod)

    **water.rs**:
    - `pub fn update_water_cells(heights: &[[u16; 128]; 128], water_flags: &mut [[u8; 128]; 128], region: &CascadeRegion, water_level: u16) -> Vec<(usize, usize)>` -- for each cell in region, if height < water_level set water flag, else clear it. Returns list of changed cells.
    - `pub fn is_water_cell(height: u16, water_level: u16) -> bool` -- height < water_level
    - Constants: `WATER_WALKABILITY_FLAG: u8 = 0x04` (cell is water)

    **mod.rs**:
    ```rust
    pub mod modify;
    pub mod cascade;
    pub mod water;

    pub use modify::{modify_height, modify_height_area, flatten_area};
    pub use cascade::{terrain_cascade, CascadeRegion, CascadeResult};
    pub use water::{update_water_cells, is_water_cell};
    ```

    Update **src/engine/mod.rs** to add `pub mod terrain;`.

    Normal calculation for cascade: for cell (x,y), compute normal as cross product of (height[y][x+1]-height[y][x-1], 2.0, 0) x (0, 2.0, height[y+1][x]-height[y-1][x]), normalized. Use wrapping indices (& 127).

    Walkability slope threshold: if abs difference between adjacent cells > 512 (height units), mark as unwalkable steep slope.
  </action>
  <verify>
    <automated>cargo test terrain:: -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/engine/terrain/modify.rs contains `pub fn modify_height`
    - src/engine/terrain/modify.rs contains `pub fn modify_height_area`
    - src/engine/terrain/cascade.rs contains `pub fn terrain_cascade`
    - src/engine/terrain/cascade.rs contains `pub struct CascadeRegion`
    - src/engine/terrain/water.rs contains `pub fn update_water_cells`
    - src/engine/terrain/water.rs contains `pub fn is_water_cell`
    - src/engine/mod.rs contains `pub mod terrain`
    - cargo test terrain passes with at least 8 tests
    - cargo test passes (all 289+ existing tests still pass)
  </acceptance_criteria>
  <done>Terrain height modification works with gradual change. Cascade updates normals, walkability, and water flags. Water/land transition based on height vs water level. All tests pass.</done>
</task>

</tasks>

<verification>
- `cargo test` passes (all existing + new terrain tests)
- `cargo test terrain` passes with modify, cascade, and water tests
- modify_height moves height gradually toward target
- terrain_cascade updates all derived data in correct order
</verification>

<success_criteria>
- Terrain can be raised/lowered gradually per tick
- Cascade updates normals, walkability, water flags after modification
- Water/land cells toggle correctly based on height vs water level
- Toroidal wrapping works for all operations
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-economy-and-combat/02-02-SUMMARY.md`
</output>
